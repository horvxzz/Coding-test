<h4>

정수 배열 numbers가 매개변수로 주어집니다. numbers의 원소 중 두 개를 곱해 만들 수 있는 최댓값을 return하도록 solution 함수를 완성해주세요.

</h4>

---

<br>

sort를 사용해 배열을 오름차순으로 정리해주고<br>
b에는 배열의 가장 큰 값 두개를 곱한 값을 저장하고<br>
s에는 배열의 가장 작은 값 두개를 곱한 값을 저장한뒤(음수*음수 경우 가능성!!)<br>
두 수를 비교해 더 큰 값을 반환한다