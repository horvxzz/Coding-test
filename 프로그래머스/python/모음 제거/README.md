<h4>

영어에선 a, e, i, o, u 다섯 가지 알파벳을 모음으로 분류합니다. 문자열 `my_string`이 매개변수로 주어질 때 모음을 제거한 문자열을 `return`하도록 `solution` 함수를 완성해주세요.

</h4>

---

<br>

`for`문으로 문자 aeiou를 하나씩 `s`에 넣어서 반복해준뒤
`my_string`안에 `s`가 있다면 `replace`로 삭제시켜준뒤 반환한다