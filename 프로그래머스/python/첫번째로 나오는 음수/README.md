<h4>

정수 리스트 num_list가 주어질 때, 첫 번째로 나오는 음수의 인덱스를 return하도록 solution 함수를 완성해주세요. 음수가 없다면 -1을 return합니다.

</h4>

---

<br>

for에 range를 이용해서 num_list의 개수만큼 반복을 해주고
만약 num_list의 원소가 음수라면 그 원소 값을 반환하고 아니라면 -1을 반환한다