<h4>

정수가 담긴 리스트 num_list가 주어집니다. num_list의 홀수만 순서대로 이어 붙인 수와 짝수만 순서대로 이어 붙인 수의 합을 return하도록 solution 함수를 완성해주세요.

</h4>

---

<br>

순서대로 이어 붙인 값을 저장할 변수 a,b를 초기화 해줍니다<br>
for문으로 num_list를 검사하고 만약 i가 짝수라면 a에 더해줍니다<br>
아니라면 b에 더해줍니다<br>
a,b는 문자열 형태이기 때문에 int를 씌워 더해 반환해줍니다